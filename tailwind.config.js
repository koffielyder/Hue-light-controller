/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Include all your component files
  ],
  theme: {
    extend: {}, // Extend Tailwind's default theme here if needed
  },
  plugins: [], // Add plugins if you need them
};
